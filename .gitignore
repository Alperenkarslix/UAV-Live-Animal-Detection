# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/

# Virtual envs
.venv/
env/
venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# App runtime
*.log
.output_*.json

# Training data & results (large, not for git)
datasets/
runs/

# Model weights (binary, large — distribute via Releases / S3 / Git LFS)
*.pt
*.onnx
*.mlpackage
*.mlmodel
*.zip

# Large downloaded videos (keep only small test clips in videos/)
videos/safari*
videos/*.f[0-9]*.mp4

# Trained model artifacts (weights, plots, batch previews, csv logs).
# Distribute via Releases / S3 / Hugging Face Hub — see README.
yolomodel/

# Frontend (Faz D)
web/node_modules/
web/dist/
web/.vite/

# Local-only DB / runtime
*.db
*.db-journal
*.db-wal
uav.db*

# Coverage
.coverage
coverage.xml
htmlcov/

# direnv
.envrc
.direnv/
